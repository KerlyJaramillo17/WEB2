/*
5.Crear una función flecha que reciba un elemento del arreglo de comidas favoritas y lo devuelva en
mayúscula*/
const comidas = [
    
]
const momentoComida = comidas.map( (comida) => { return comida.momento; })
console.log( "ceviche".toUpperCase() );
